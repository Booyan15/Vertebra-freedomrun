Generated transparent exercise frame sequences live here.

The app expects:

`/public/exercises/<exercise-slug>/frame-01.webp`
`/public/exercises/<exercise-slug>/frame-02.webp`
and so on.

These files are served at runtime as:

`/exercises/<exercise-slug>/frame-01.webp`

Keep each sequence visually consistent: same model, transparent background, no text embedded in the image.
